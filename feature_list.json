{
  "project": "draw-a-card-front",
  "description": "Expo (React Native) app for the TCG card portfolio, scanning, social feed, and trading platform — iOS, Android, and web from one codebase.",
  "rules": {
    "one_feature_at_a_time": true,
    "require_tests_to_close": true,
    "require_approved_spec_to_implement": true,
    "valid_status": [
      "pending",
      "spec_ready",
      "in_progress",
      "done",
      "blocked"
    ],
    "sdd_required_when": "feature has \"sdd\": true"
  },
  "features": [
    {
      "id": "001-registration-kyc",
      "sdd": true,
      "status": "done",
      "branch": "001-registration-kyc",
      "spec_dir": "specs/001-registration-kyc",
      "created": "2026-08-02",
      "updated": "2026-08-04",
      "blocked_reason": null,
      "notes": "DONE 2026-08-04 (human-directed close) -- 31/31 in-scope tasks [X], 181 tests passing, ./init.sh RESULT: SUCCESS (10/10 stages incl. the new iOS/Android bundle exports), tsc clean. Ships: personal + Tienda registration, phone verification with SMS autofill variants, the profile step (typed CURP/RFC fields per the re-scope), the KYC status gate, and the first-run tutorial. FOUR remaining tasks were MOVED OUT to 003-registration-kyc-completion rather than dropped: T021 (manual smoke -- web blocked on backend 006-web-cors, iOS partially verified), T022/T023 (US3 session persistence, deferred to backend 004-session-authentication), T035 (real tutorial endpoints, now that backend 002-onboarding-tutorial-state shipped them). CAVEATS THE HUMAN SHOULD KNOW BEFORE MERGING: (a) code-reviewer's second review returned CHANGES_REQUESTED on a blocking finding (setCurrentUserId never called -> every /identity/me/* and /phone/* call would 401); that was fixed as T033, but T033 and T034 landed AFTER that review and have NOT been re-reviewed by code-reviewer. (b) The end-to-end flow has never been run green against a live backend: web is blocked by the backend's missing CORS (registered there as 006-web-cors), and the happy path additionally needs real EXPO_PUBLIC_SUPABASE_URL/ANON_KEY plus the backend pointed at the same Supabase project (it currently runs AUTH_PROVIDER_MODE=mock, which creates a fake authProviderId the client cannot sign in against). Registration itself IS confirmed working against the real backend (HTTP 201 via curl and from the iOS simulator). (c) Four 'green tests, broken app' bugs were found by manual/real-backend testing that the test suite did not catch (T031 no session ever established, T033 X-User-Id never set, T034 unhandled sign-in rejection, plus an ExpoLinking native-module mismatch) -- treat 181 passing tests as necessary, not sufficient. PRIOR HISTORY -- RE-SCOPED 2026-08-04 (T001-T003 already [X], implementation resumed at new T004). task-implementer's mandatory backend cross-check during T001-T003 found the approved spec/plan/tasks targeted a backend scope that no longer exists: (1) backend descoped ALL KYC document handling out of its 001 into 002-kyc-document-verification (pending, unspec'd) on 2026-08-03 -- no presign endpoint, no object storage, no KYC provider; (2) backend has no path off kycStatus: pending at all (FR-006: KYC gates money paths only, not implemented here); (3) real backend flow is 3 calls -- POST /identity/register(/business) (email/password/phone/username only) -> POST /identity/phone/verify -> POST /identity/me/profile (nombre/apellidoPaterno/apellidoMaterno?/birthDate/nationality/curp/rfc as typed fields + ToS/privacy), not the assumed 2-step doc-upload flow; (4) business fields (commercialName/rfc/fiscalAddress) belong at the profile step, not registration. Orchestrator independently re-verified all findings against the backend's specs/001-user-registration-kyc/spec.md, prisma/schema.prisma, and feature_list.json (backend 001 is done, 117 tests). Human re-scope decisions encoded 2026-08-04: (A) document upload moved out to new frontend feature 002-kyc-document-verification (registered below, pending, blocked on backend 002 being spec'd); (B) kycStatus: pending now passes through to the main app (resolveKycRoute/KycStatusScreen stay, but only route to kyc-status on rejected or a fetch-failure -- rejected is unreachable against the current backend, built-and-tested via mocked fixtures, not dead code); (C) added a profile-step screen gated behind phone verification. spec.md/plan.md/tasks.md all amended accordingly; FR-004 rewritten (typed profile fields, was documents), FR-009 rewritten (routing gate matrix, decision B), original document-related FR-004/FR-008/SC-004/AS/platform-notes/edge-cases preserved verbatim under each file's 'Deferred to feature 002' section. tasks.md: T001-T003 stay [X] with unchanged numbering; new T004/T005 are follow-ups to already-completed T002 (remove IdentityDocument -- moved to 002, not kept as forward declaration; add backend-mirroring User fields) and T003 (widen resolveKycRoute's route union/branches per decision B) respectively; former T005/T006/T014/T015/T016/T017 (presign/upload/confirm, camera-upload adapter, permission screen, doc-upload screens) relocated verbatim to a 'Deferred to feature 002' section at the bottom of tasks.md. Also notes an unresolved constraint (not solved by this feature): backend /identity/me/* and /identity/phone/* authenticate via a dev-only X-User-Id header until backend 003-session-authentication (pending) ships -- recorded in spec.md Assumptions, affects US3 and every authenticated call. See progress/current.md for the full session log."
    },
    {
      "id": "002-kyc-document-verification",
      "sdd": true,
      "status": "pending",
      "spec_dir": "specs/002-kyc-document-verification",
      "created": "2026-08-04",
      "updated": "2026-08-04",
      "blocked_reason": null,
      "notes": "Registered 2026-08-04 as part of re-scoping 001-registration-kyc: KYC document upload (official ID + proof of life capture, presign->PUT->confirm upload flow, the pre-permission explanation screen, camera-permission/offline-retry edge cases) was moved out of 001 into this feature, mirroring the same split already made on the backend (backend's KYC-document feature, RENUMBERED to 003-kyc-document-verification as of 2026-08-04 -- the backend inserted 002-onboarding-tutorial-state ahead of it, so every 'backend 002' reference written earlier in this repo means backend 003; still pending/unspec'd). No spec.md written yet -- deliberately not spec'd until the backend's own 003 is spec'd first, since this feature's contract (endpoints, field names, kycStatus transition mechanics) has no backend counterpart to mirror yet. All the original 001 requirements/acceptance-scenarios/platform-notes/edge-cases/tasks this feature should start from are preserved verbatim in specs/001-registration-kyc/spec.md's and tasks.md's 'Deferred to feature 002' sections -- read those first. Also owns: kycStatus transitions away from pending (verified/rejected), enforcement of the money-path KYC gate (sale/withdrawal only, not card-for-card trades, per backend FR-006), and whatever CTA destination 001's KycStatusScreen's 'Resubmit documents' placeholder route should eventually point to."
    },
    {
      "id": "003-registration-kyc-completion",
      "sdd": true,
      "status": "pending",
      "spec_dir": "specs/003-registration-kyc-completion",
      "created": "2026-08-04",
      "updated": "2026-08-04",
      "blocked_reason": null,
      "notes": "Created 2026-08-04 to hold the four tasks that were still open when 001-registration-kyc was closed at the human's direction. These are NOT abandoned work -- each is carried over verbatim, with its full context, in specs/001-registration-kyc/tasks.md's 'Moved to feature 003' section; read that first, it is the source of truth for scope. Contents: (1) T035 -- switch tutorial completion from local-only expo-secure-store to the real GET /identity/me/tutorials + POST /identity/me/tutorials/complete, which the backend's 002-onboarding-tutorial-state shipped on 2026-08-04. This is a genuine FR-007 correctness fix, not cleanup: local-only completion is lost on reinstall and does not follow a user across devices, so the tutorial replays for a user who has already seen it, meeting 'once per install' but not FR-007's 'once per user'. THE ONLY ONE OF THE FOUR NOT BLOCKED ON ANYTHING EXTERNAL -- start here. (2) T022/T023 -- US3 session persistence, deferred by human decision on 2026-08-04 because it cannot work until the backend's 004-session-authentication ships: the backend has no full-profile GET (only GET /me/kyc-status, returning { kycStatus } alone), and X-User-Id is in-memory only, so every cold boot fails the status fetch and lands on FR-010's retry screen. BLOCKED on backend 004. (3) T021 -- the manual smoke check 001 never completed. Web is BLOCKED on the backend's missing CORS (tracked there as 006-web-cors); iOS is partially verified (register screen confirmed against the live backend, HTTP 201) but the happy path additionally needs real EXPO_PUBLIC_SUPABASE_URL/ANON_KEY and the backend moved off AUTH_PROVIDER_MODE=mock onto the same Supabase project. Android was never exercised -- no Android SDK on the dev machine as of 2026-08-04. Also worth folding in here: a code-reviewer pass over 001's T033/T034, which landed after the last review and were never re-reviewed. No spec.md written yet."
    },
    {
      "id": "004-home-scan-shell",
      "sdd": true,
      "status": "done",
      "branch": "004-home-scan-shell",
      "spec_dir": "specs/004-home-scan-shell",
      "created": "2026-08-04",
      "updated": "2026-08-05",
      "blocked_reason": null,
      "notes": "DONE 2026-08-05 -- all 22/22 tasks [X], reviewed batch-by-batch by code-reviewer (final verdict APPROVE WITH NITS), ./init.sh RESULT: SUCCESS (10/10 stages, only the pre-existing expo-doctor/native-dep-alignment warnings that predate this feature), 233/233 tests passing, tsc clean. Ships: the persistent nav shell (native <Tabs> on iOS/Android, responsive sidebar>=768px/bottom-bar<768px on web via the .web.tsx convention), the Home/Scan screen (top-left Amigos quick-access pill, top-right four inert placeholder controls, centre '+' card), the /scan and Amigos/Social placeholder route boundaries, and app/index.tsx's removal in favor of app/(app)/index.tsx as 001-registration-kyc's 'main' gate destination. FR-009 held throughout -- zero diff to resolveKycRoute()/useKycGate()/KYC_ROUTE_TARGETS/app/_layout.tsx, independently re-confirmed at every review round including the final one. FR-010 held -- zero backend calls anywhere in this feature's files, independently grepped at the final review. VERIFICATION, PRECISELY STATED (per this repo's own 'green tests, broken app' history -- do not overstate): web was exercised extensively and repeatedly against a REAL running `npm run web` dev server via headless Chrome driven over raw CDP (real DOM reads, real keyboard Tab-order walks, real OS-level window resizes proving no-reload, real click-throughs) at nearly every task, not just Jest/RNTL. A real iOS Simulator (booted iPhone 17 Pro, and for T020/T021 also iPad Pro 13-inch) was used with real Expo Go installs and real device screenshots at T015 and T020/T021 -- this caught and fixed two genuine runtime-only bugs neither type-check nor the web export could have caught: (1) a landscape-viewport card-clipping bug (root View->ScrollView fix in HomeScreen.tsx -- DISCLOSED DESIGN DEVIATION, human-confirmed 2026-08-04 at an escalation gate: the fix scrolls the WHOLE screen rather than just the top-right control stack as spec.md's Edge Cases section originally, illustratively suggested; spec.md's Edge Cases section has been amended by the human directly to state the requirement as the outcome rather than the original illustrative mechanism); (2) an iOS status-bar/Dynamic-Island overlap (useSafeAreaInsets() fix, react-native-safe-area-context was already a dependency but had never actually been called anywhere in the codebase before this). ONE GENUINE OPEN GAP FROM T015, DISCLOSED, NEVER CONCLUSIVELY RESOLVED: reaching Amigos/Social ON NATIVE during manual verification used Expo Go's cold deep-link mechanism as a substitute for a real tap (no Accessibility/UI-automation permission was available in the sandbox to simulate an actual tap); one resulting screenshot showed the native tab bar not visible on the Amigos screen reached that way -- NOT reproduced via a real tap, NOT attributed conclusively to either a real app bug or a deep-link artifact. Recommend a human with real device access specifically check: does tapping the Amigos tab from a warm Home screen keep the tab bar visible on the Amigos screen? T019 (top-right controls' manual a11y pass) was WEB-ONLY -- no simulator booted for that task, no real VoiceOver/TalkBack session was ever run at any point in this feature, disclosed honestly rather than assumed covered. ANDROID WAS NEVER AVAILABLE IN THIS ENVIRONMENT AT ANY POINT -- no emulator/SDK, zero Android-specific manual verification was performed for this entire feature (inferred-safe via no .android.tsx files and cross-platform-only RN primitives used, but never actually checked). ONE ACKNOWLEDGED, ACCEPTED, NON-BLOCKING LOOSE END surfaced three times across review rounds and explicitly flagged again at the final gate, recorded here per code-reviewer's request rather than silently dropped a fourth time: WebSidebarNav/WebBottomBarNav (T010/T011) each wrap their own independent expo-router <Slot/>, so app/(app)/_layout.web.tsx's sidebar<->bottom-bar switch on a breakpoint crossing unmounts/remounts the active screen subtree, silently resetting any of that screen's local component state (concretely: TopRightControls' 'Not yet available' toggle resets on a resize-triggered layout switch). This does NOT violate spec.md's binding AS6 ('without losing the active destination') since the active ROUTE survives (it lives in expo-router's context above this layout) -- only ephemeral UI feedback state resets, no data/navigation loss. tasks.md's T012 own descriptive text ('without unmounting/remounting the active screen's state') is not literally true of what shipped; low real-world severity, orchestrator-acknowledged here as the explicit sign-off code-reviewer asked for, not fixed in this feature -- a candidate for a small follow-up (hoist a single shared <Slot/> above the layout ternary) if it's ever judged worth the churn. See progress/history.md for the full session log. PRIOR notes (spec-writer's pre-implementation summary) preserved below. spec.md/plan.md/tasks.md written 2026-08-04 by spec-writer -- zero blocking [NEEDS CLARIFICATION] markers, so NOT blocked. ONE open design decision recorded with a chosen default instead of a blocking marker, per the human's own framing ('so the human can decide at the approval gate'): the web nav treatment (spec.md Clarifications) defaults to Option C -- persistent left sidebar >=768px, native-equivalent bottom bar below -- flagged explicitly for the human to confirm or override when reviewing this feature; every other artifact is written so only that one line item would need to change if overridden. Also resolved (non-blocking, a reasonable default exists): the wireframe's top-left 'Amigos' pill navigates to the same Amigos destination as the shell's Amigos tab rather than a second competing entry point (FR-008) -- flagged in spec.md's Design note in case the human intended something else. Scope confirmed against feature_list.json's original request notes (preserved below) plus AGENTS.md/constitution.md/docs/conventions.md/docs/verification.md/specs/001-registration-kyc's pattern: 4 user stories (P1 shell+landing, P1 '+' -> scanner-stub navigation, P2 Amigos/Social reachability, P3 top-right inert controls), 22 tasks, zero backend calls (explicit in spec.md and plan.md -- this is pure UI-shell work, no Draw-a-card backend counterpart to check against). Integration point verified: 001's resolveKycRoute()/useKycGate()/KYC_ROUTE_TARGETS are read-only dependencies here (FR-009) -- this feature only replaces what the existing 'main' route renders (app/index.tsx's scaffold placeholder -> app/(app)/index.tsx inside a new tab/shell route group), does not touch the gate itself. No new runtime dependency needed (plan.md Research Decisions: @react-navigation/bottom-tabs already ships transitively with the installed expo-router, @expo/vector-icons already ships transitively with expo) -- confirmed against node_modules directly, not assumed. Constitution Principle V exception documented and justified in plan.md's Constitution Check: new src/features/navigation/ module has no backend bounded context to mirror (it's the cross-cutting shell chrome), while the scanner-entry card/stub and Amigos/Social placeholders correctly reuse the existing src/features/scanner/ and src/features/social/ scaffolds. ORIGINAL REQUEST (verbatim, preserved for reference): Wireframe contents, top to bottom: (a) top-left a pill-shaped 'Amigos' button; (b) top-right a vertical stack of four small controls, labelled in the sketch as 'ENG/ESP', 'USD/MXN', 'Notifications', 'Messages'; (c) dead-centre a large vertically-oriented rounded rectangle (card aspect ratio) containing a single '+' glyph; (d) along the bottom edge a wide rounded bar labelled 'social'. IN SCOPE: the persistent navigation shell containing three destinations -- Amigos, the centre Scan/home screen, and Social -- rendered as a BOTTOM TAB BAR on iOS/Android and, per the human's explicit instruction, as a modern web-appropriate navigation on web (the human said 'for web try to implement it in a modern way' -- treat the exact web treatment as a design decision for the spec to settle and clarify with the human if genuinely ambiguous, but it must satisfy constitution Principle VII responsiveness/keyboard-nav and Principle IV's rule that the platform split uses the .web.tsx convention rather than inline Platform conditionals); the centre home screen itself with the card-shaped '+' affordance; and the four top-right controls. OUT OF SCOPE (explicit human instruction -- 'ignore the amigos and social but add the tab'): any Amigos or Social screen content. Those two destinations get reachable placeholder screens only. ALSO OUT OF SCOPE: the scanner itself -- the human stated it is 'another feature'. The '+' card is an affordance that NAVIGATES to the scanner route; this feature defines and wires that route boundary and may stub the destination, but implements no camera, no capture, no recognition. The four top-right controls are PLACEHOLDERS TOO: render them with correct affordance, position, accessibility labels and tap targets, but no language switching, no currency conversion, no notification feed, no messaging -- each is a no-op or a stub destination. Note the language and currency controls imply future i18n (ENG/ESP) and multi-currency (USD/MXN) features that do not exist yet; the spec should record them as forward dependencies, not build them. INTEGRATION POINT: app/index.tsx is still the scaffold placeholder from repo setup, and 001-registration-kyc's routing gate (src/features/identity resolveKycRoute) already routes a verified user to the app root -- so this feature is what that gate lands on. Check how 001 wires the post-KYC landing before changing routing."
    },
    {
      "id": "005-login",
      "sdd": true,
      "status": "done",
      "branch": "005-login",
      "spec_dir": "specs/005-login",
      "created": "2026-08-05",
      "updated": "2026-08-05",
      "blocked_reason": null,
      "notes": "DONE 2026-08-05 -- all 20/20 tasks [X], reviewed batch-by-batch by code-reviewer across 9 review rounds (T001/T002, T003/T004, T005, T006 + a follow-up FR-002 regression test, T007, T008/T009, T010, T011/T012, T013/T014 [one CHANGES_REQUESTED -> fix -> APPROVE cycle], T015, T016, T017/T018, T019) plus a final feature-level review verdict of READY TO BE MARKED done, ./init.sh RESULT: SUCCESS (10/10 stages, only the pre-existing expo-doctor/native-dep-alignment warnings that predate this feature), 302/302 tests passing across 44 suites, tsc clean. Ships: an email+password /login screen for returning users (src/features/identity/SignInForm.tsx + LoginScreen.tsx + app/(auth)/login.tsx, reusing 001's signInWithPassword()/NETWORK_SIGN_IN_ERROR_MESSAGE byte-for-byte unchanged); a full forgot-password flow entirely as local view-state on /login (request-reset -> reset-with-code -> back to sign-in, src/features/identity/RequestPasswordResetForm.tsx + ResetPasswordForm.tsx, src/domain/passwordReset.ts, and src/lib/supabase-client.ts's requestPasswordReset()/createPasswordRecoverySession() -- a second, throwaway, persistSession:false Supabase client instance, independently proven via a reviewer-performed sabotage-and-restore mutation test to never touch the shared singleton the KYC gate observes); and the one deliberate, human-approved edit to 001's gate wiring (KYC_ROUTE_TARGETS.unauthenticated '/register' -> '/login' in src/features/identity/useKycGate.ts, exactly one line -- resolveKycRoute()/src/domain/kyc-gate.ts and app/_layout.tsx's KycGate independently re-confirmed byte-for-byte unchanged from main at every review round including the final one, via git diff, not assumed). All three human-approved scope decisions held exactly as approved, independently re-verified in the final review: (1) password reset calls supabase.auth.resetPasswordForEmail() directly, never the backend's own POST /identity/password-reset (grepped, zero hits); (2) the entire forgot-password flow has no new route -- grepped app/ for anything reset/forgot-related, only login.tsx's own mode state exists; (3) the KYC gate diff is exactly the one line. Social sign-in (Google/Apple OAuth) and an explicit 'remember me' toggle were confirmed NOT built (grepped, zero hits), matching the human's explicit scoping decision to leave both out. All FR-001..FR-010 traced to real, independently-reconfirmed passing tests (see the final review's FR table in progress/review_005-login.md). ONE REAL BUG CAUGHT AND FIXED DURING THIS FEATURE, not shipped silently: the first T013/T014 review round returned CHANGES_REQUESTED on a genuine spec-correctness gap -- LoginScreen.tsx's handleRequestReset unconditionally advanced to the code-entry screen regardless of whether the reset-code request actually succeeded, silently swallowing a network failure and showing the user a code-entry screen as if an email had been sent (violating spec.md's explicit Edge Cases requirement and SC-002); task-implementer had self-disclosed this gap rather than hiding it, and the fix (branch on the result, add a serverError prop, keep the user on request-reset mode with the error shown) was independently re-verified before APPROVE. A second, non-blocking finding from the same round (the anti-enumeration confirmation copy being set then instantly unmounted, never actually visible to a user) was resolved by adding a static, always-shown confirmation line to ResetPasswordForm.tsx (option (b) of two reviewer-offered choices, orchestrator-directed). VERIFICATION, PRECISELY STATED (per this repo's own 'green tests, broken app' history -- do not overstate): web was exercised repeatedly against a REAL running `npm run web` dev server via the Claude Browser tool at three checkpoints (T007 MVP smoke, T015 US2 smoke, T017/T018 live a11y/responsive follow-up) -- confirmed live: cold boot lands on /login not /register; the Create-account link resolves to /register; client-side validation blocks submission with zero network calls; the T013/T014 Fix-1 network-failure-stays-in-mode behavior, reconfirmed live not just via mocked unit test; 'Back to sign in' clears all residual state; real keyboard tab order across sign-in and request-reset modes with a genuine non-suppressed focus ring; no horizontal overflow at a 375px viewport and correct desktop max-width capping. NOT verified live in this environment, honestly disclosed rather than implied: this sandbox's .env has empty EXPO_PUBLIC_SUPABASE_URL/ANON_KEY (same pre-existing limitation 001-registration-kyc already disclosed), so credentials-differentiation (wrong-password vs unregistered-email producing an identical real Supabase error), the successful-sign-in landing screen, and the full reset-with-code happy path (real emailed code, real verify/update against Supabase) were never run against a live Supabase project -- covered only at the unit/component-test level instead (already reviewed/approved). iOS Simulator was unavailable in this environment at every attempt (Xcode not selected, needs sudo + the human's password) -- disclosed each time, never silently skipped or assumed covered. Android was never available either (no emulator/SDK), consistent with every prior feature in this repo. ONE EXPLICIT, NON-BLOCKING RECOMMENDATION FROM THE FINAL REVIEW, recorded here rather than dropped: the reset-with-code flow's real live-Supabase, real-emailed-code behavior has never actually been watched working end-to-end in this environment -- recommend a real manual pass against a live Supabase project (and iOS/Android when device access is available) as a near-term post-merge follow-up; this mirrors, not introduces, the exact live-backend/live-device gap 001-registration-kyc already shipped under, so it was not treated as a reason to withhold done status. HUMAN REMINDER (explicitly requested to not be forgotten): the Supabase project dashboard's 'Reset Password' email template must include `{{ .Token }}` for the code-based reset flow to actually deliver a code by email -- a one-time, out-of-repo dashboard configuration step, documented in spec.md's Assumptions and src/features/identity/README.md, not application code and not a blocker for this feature's done status. See progress/history.md for the full session log. PRIOR notes (spec-writer's pre-implementation summary, from the spec_ready gate) preserved below. SPEC/PLAN/TASKS WRITTEN 2026-08-05 by spec-writer -- zero [NEEDS CLARIFICATION] markers, so no /speckit-clarify pass was needed; status flipped to spec_ready, awaiting human approval at the gate. Backend cross-check performed against the sibling Draw-a-card repo's specs/001-user-registration-kyc/spec.md and src/modules/identity/routes.ts directly (not assumed): confirmed there is no backend sign-in/session endpoint of any kind (sign-in is 100% Supabase-SDK, nothing extra expected of the backend, matching the kickoff brief) -- but ALSO found the backend already ships its own POST /identity/password-reset (its own FR-008/User Story 3 'Forgot password', already done/tested), which this frontend feature deliberately does NOT call, calling supabase.auth.resetPasswordForEmail() directly instead, per Constitution Principle III's literal text and the human's own 'Supabase reset-password email' scoping-gate wording -- recorded as spec.md Clarifications 'Recorded default 1', flagged for explicit human confirmation/override at the approval gate (Option B, reusing the backend endpoint, is documented as the alternative). SECOND, more load-bearing finding from tracing app/_layout.tsx's KycGate component and expo-router's actual Redirect implementation (node_modules/expo-router/build/link/Link.js, read directly, not assumed): a classic magic-link/deep-link password-reset flow would establish a session visible to the shared Supabase client the KYC gate observes, and the gate would very likely redirect away from a dedicated /reset-password route before the user could enter a new password -- fixing that within the human's explicit 'keep the diff to the one KYC_ROUTE_TARGETS.unauthenticated mapping only' constraint would need a further, unplanned change to the gate's own wiring. Resolved instead (spec.md Clarifications 'Recorded default 2', also flagged for confirmation) by keeping the ENTIRE forgot-password flow as local view-state on /login itself (no new route at all) and running the code-verification/set-new-password step against a second, throwaway, non-persisted Supabase client instance (persistSession:false) created just for that step, so no session the gate can observe is ever created mid-flow -- this reuses the existing 'type a code that arrived out-of-band' UX already shipped for SMS phone verification (CodeInput/VerifyPhoneScreen) rather than inventing deep-linking (a genuinely new, more complex capability for this repo) for a P2 story. The diff to 001's gate wiring stays exactly the one permitted KYC_ROUTE_TARGETS.unauthenticated line ('/register' -> '/login'); resolveKycRoute()'s branch logic and every other KYC_ROUTE_TARGETS entry are confirmed untouched in tasks.md's T006 instructions. Also spelled out plainly, per the kickoff brief's explicit instruction not to silently imply otherwise: because nothing in this feature's sign-in path ever learns the backend's User.id (unlike registration, whose response body IS the created User), EVERY successful sign-in through this feature -- not only a literal cold app-launch -- currently lands on 001's existing FR-010 retryable 'couldn't load your verification status' screen rather than the main app, per the still-unresolved dev-only X-User-Id limitation tracked as 003-registration-kyc-completion T022/T023 and backend 004-session-authentication; this feature does not attempt to fix that. specs/005-login/spec.md: 3 user stories (P1 sign-in + the routing change/MVP, P2 forgot-password, P3 create-account link), FR-001..FR-010, SC-001..SC-005, two Clarifications entries with full rejected-alternatives tables (both flagged for the human to confirm or override at the gate -- everything else in the spec is independent of which way either is decided). plan.md: Constitution Check all PASS (Principle III is the deciding factor behind Clarifications' Recorded default 1), Research Decisions covering sign-in reuse (src/lib/supabase-client.ts's existing signInWithPassword()/NETWORK_SIGN_IN_ERROR_MESSAGE, UNCHANGED), no-hardcoded-post-login-navigation (defers entirely to the existing, unmodified useKycGate()/resolveKycRoute()), the throwaway-client password-reset design in full, shared schemas (new passwordSchema factored out of personalRegistrationSchema with no behavior change), and reuse of CodeInput/FormField (no new UI primitive). tasks.md: 20 tasks (T001-T020) across a small Foundational phase (schemas/login.ts, scoped only to what US1/the MVP needs) and three user-story phases; every task names its serving FR(s) and file paths, and T006 (the one-line KYC_ROUTE_TARGETS edit) has an explicit self-check instruction not to let its diff grow. HUMAN APPROVAL GATE: status is now spec_ready; per AGENTS.md, sdd-orchestrator must pause here for explicit human go-ahead -- and should specifically relay the two flagged Clarifications decisions (password-reset caller: direct SDK vs. the backend's existing endpoint; forgot-password mechanism: in-place code entry vs. a magic-link/deep-link route) for confirmation, not just the routing-change edit. PRIOR (kickoff) notes preserved below. Registered 2026-08-05 at the human's request ('make a new feature, this feature will be the login view'). THE GAP THIS FILLS: 001-registration-kyc shipped registration (which auto-signs-in the brand-new user via src/lib/supabase-client.ts's signInWithPassword) but NO sign-in screen for a RETURNING user -- src/features/identity/useKycGate.ts's KYC_ROUTE_TARGETS currently maps `unauthenticated` to `/register`, so a user who already has an account and comes back to a signed-out app is sent to a registration form that will fail with the backend's EmailTaken (409). SCOPE, decided by the human at a scoping gate on 2026-08-05 (two questions asked, both answered): (1) email + password sign-in form, PLUS a forgot-password flow (Supabase reset-password email + a set-new-password screen) -- explicitly chosen over the 'just the sign-in form' option, so password recovery is IN scope, not deferred; (2) the unauthenticated landing MOVES to the new login screen: change KYC_ROUTE_TARGETS.unauthenticated from '/register' to '/login', with the login screen carrying a 'Create account' link to /register. This is a deliberate, human-approved edit to 001's gate wiring (KYC_ROUTE_TARGETS only) -- note that 004-home-scan-shell held FR-009 'zero diff to the gate' as a hard constraint, so this feature is the first one permitted to touch it; keep the diff to that one route-target mapping and do not alter resolveKycRoute()'s branch logic in src/domain/kyc-gate.ts. NOT CHOSEN by the human, so OUT OF SCOPE: social sign-in (Google/Apple OAuth) and an explicit 'remember me' toggle (session persistence already works via the expo-secure-store adapter in src/lib/supabase-client.ts). EXISTING PIECES TO REUSE RATHER THAN REBUILD -- the spec/plan should check each against the code before assuming: src/lib/supabase-client.ts already exports signInWithPassword() with the T034 network-failure handling (it MUST NEVER THROW -- read its doc comment before touching it) and NETWORK_SIGN_IN_ERROR_MESSAGE; src/features/identity/FormField.tsx and RegistrationForm.tsx are the established form/validation patterns; src/domain/schemas.ts holds the existing validation schemas. Constitution Principle IV applies as always -- sign-in/reset business logic belongs in src/domain (pure) with the Supabase adapter calls in src/lib, never inline in the screen. BACKEND CROSS-CHECK REQUIRED before the plan is finalized (this repo's own history: 001's approved spec targeted a backend scope that had moved): sign-in and password reset here go through the Supabase Auth SDK directly rather than the Draw-a-card backend, but confirm against the backend repo whether it expects/needs anything on sign-in, and note the still-unresolved dev-only X-User-Id constraint recorded in 001's spec.md Assumptions -- the backend's /identity/me/* calls authenticate via that in-memory header until backend 004-session-authentication ships, which means a user who signs in on a COLD BOOT will have a valid Supabase session but no backend user id, landing on FR-010's retryable status screen. That is a known pre-existing limitation (tracked as 003-registration-kyc-completion items T022/T023), not something this feature is required to solve, but the spec must state explicitly what a successful login lands on given it."
    },
    {
      "id": "006-visual-identity",
      "sdd": true,
      "status": "done",
      "branch": "006-visual-identity",
      "spec_dir": "specs/006-visual-identity",
      "created": "2026-08-05",
      "updated": "2026-08-05",
      "branch_cut_from": "main@777bb9e",
      "blocked_reason": null,
      "notes": "DONE 2026-08-05 -- all 54 tasks (55 checkbox lines incl. T024a) [X], reviewed batch-by-batch by code-reviewer across 20 review rounds (T001; T002-T009; T011-T016 [one CHANGES_REQUESTED on two FR-001 magic-literal findings in OrDivider/StatusPill -> fixed -> APPROVE]; T017-T022; T010; T023/T024/T024a [APPROVE WITH NITS -- disclosed pre-existing FormField error-text hex, later resolved]; T025-T027; T028/T029; T030/T031 [ripple into LoginScreen.test.tsx/login.test.tsx scrutinized, confirmed narrow]; T032/T033 [same ripple pattern, same scrutiny]; T034-T037 [the single most regression-critical task -- LoginScreen.tsx wiring, FR-006 guard independently re-verified]; T038-T042; T043/T044; T045/T046 [camera-import guard migration, sequencing verified]; T047-T049; T050/T051 [resolved the FormField error-text nit via a new computed colors.text.danger token, added ScrollView fix for a genuine clipping bug]; T052-T054 [final regression gate]), plus a final feature-level APPROVE confirming all 55 checkboxes and CHECKPOINTS.md C1-C6. ./init.sh RESULT: SUCCESS (10/10 stages, only the same pre-existing expo-doctor/native-dependency-alignment warnings that predate this feature -- independently confirmed zero new drift from this feature's three new dependencies), 394/394 tests passing across 63 suites, tsc clean. Ships: (a) src/theme/ -- a shared design-token module (colors/typography/geometry/shadows/contrast), structured for a future dark theme without touching call sites, semantic names only, zero raw hex/magic-literal duplication (SC-001, grep-verified repeatedly); (b) six shared primitives in src/features/ui/ (BrandMark, PrimaryButton, SecondaryButton, OrDivider, StatusPill) plus the restyled Field via src/features/identity/FormField.tsx/.web.tsx (extended in place per the explicit instruction, not a parallel component); (c) the login screen fully restyled on mobile+web (LoginScreenChrome, SignInForm, RequestPasswordResetForm, ResetPasswordForm, LoginScreen) with zero regression to 005-login's FR-006 (LoginScreen never calls useRouter() on successful sign-in -- independently re-verified at every relevant review round via direct source grep, not assumed) or the forgot-password-as-local-state design; (d) the scan screen's visual shell on mobile+web (Viewfinder, ScanSearchField, UploadDropzone, EmptyResultsPanel, RecentScansList, ScanShellScreen/.web.tsx replacing the retired ScanPlaceholderScreen) with a migrated, independently-reconfirmed-strict camera-import source-inspection guard (SC-004, zero expo-camera/expo-image-picker import anywhere under src/features/scanner/) and FR-008's placeholder recent-scans rows clearly commented as such with zero src/domain import/fetch; (e) an i18n layer (src/domain/i18n/ -- zero React Native imports, confirmed -- plus src/features/i18n/LocaleContext.tsx) carrying complete, key-parity-tested Spanish+English dictionaries for both screens, default locale hardcoded to Spanish (FR-012) and explicitly documented as a 007-localization placeholder, with the useLocale().setLocale seam 007 builds its picker on top of. THREE HUMAN-APPROVED DESIGN DECISIONS FROM THE SPEC_READY GATE, ALL CONFIRMED SHIPPED EXACTLY AS APPROVED: (1) Playfair Display 700 via @expo-google-fonts/playfair-display + expo-font, loaded behind the same minimal loading-guard pattern KycGate already used, referenced only via typography.display.fontFamily -- independently confirmed at the T010 review (root-layout wiring, the highest-generic-regression-risk task, extra scrutiny paid to zero-diff on KycGate/useKycGate()/resolveKycRoute()); (2) four contrast-adjusted token values (text.secondary #646B78, text.placeholder #6D7787, viewfinder.hintText #9CA3AF as a distinct token, text.link #247B3D, accent.priceGreen #1C844A) plus a fifth value (colors.text.danger, added during Polish to resolve a disclosed nit) -- all computed via a real WCAG contrastRatio() implementation (src/theme/contrast.ts), regression-tested in contrast.test.ts against the real colors export (not hardcoded expected numbers), independently re-verified at the final review round; (3) /scan stays the standalone route 004-home-scan-shell built, no sidebar/tab bar added, restyled 'Back' affordance (still calling router.back(), byte-for-byte behaviorally unchanged) is the sole return path -- 004 US2 AS2 preserved, independently re-diffed at the T047-T049 review. ONE DISCLOSED, ACCEPTED, EXPECTED SIDE EFFECT: restyling FormField.tsx in place (per explicit instruction, not a parallel component) also visually changes RegistrationForm/VerifyPhoneScreen/ProfileForm's rendered field appearance -- FormFieldProps' shape and every one of those three screens' call sites and test suites were independently re-run at the T023/T024/T024a review and again at the final T052-T054 regression gate, zero behavioral regression, zero test-file change needed (all three already asserted behavior/role/text, not visual literals, confirmed by direct inspection). TWO GENUINE BUGS CAUGHT AND FIXED DURING THIS FEATURE, DISCLOSED NOT HIDDEN: during Polish (T050/T051), task-implementer found and fixed (a) LoginScreenChrome/ScanShellScreen had no ScrollView, so tall content on a short viewport was silently clipped with no way to reach it -- fixed with ScrollView + keyboardShouldPersistTaps='handled', independently re-verified against the T025-T027 children-passthrough guard and the FR-006 guard, zero regression; (b) Viewfinder's gear chip used accessibilityElementsHidden/importantForAccessibility, which react-native-web does not forward to the DOM -- swapped to aria-hidden, a change the reviewer verified is technically correct on BOTH native and web by reading the actual react-native/react-native-web source directly (RN's View component maps aria-hidden to the same native mechanism; react-native-web forwards it to a real DOM attribute), not accepted on the implementer's claim alone. VERIFICATION, PRECISELY STATED (per this repo's own 'green tests, broken app' history -- do not overstate): every task in this feature was verified via component/unit tests (RNTL, real rendered output/behavior, not smoke tests) plus repeated full-suite + full ./init.sh (all 8 stages, no skip flags) reruns, independently reproduced by code-reviewer at every single review round rather than accepted from task-implementer's report. NOT verified live in this environment, honestly disclosed at every relevant task (T037, T049, T050/T051) rather than silently skipped: no headless browser or iOS/Android simulator was available in this sandbox at any point in this feature -- manual smoke checks (Level 3) substituted component tests asserting mode/breakpoint-gated rendering plus clean dev-server-boot/bundle-export checks, and every report said so explicitly rather than implying a live check occurred. This mirrors, not introduces, the identical simulator/live-backend gap every prior feature in this repo (001, 004, 005) has already disclosed and shipped under. RECOMMENDATION FOR A POST-MERGE FOLLOW-UP, not a blocker: a real manual pass on iOS/Android simulators and a real browser (pixel-level rendering, live resize across the 768px breakpoint, live VoiceOver/TalkBack announcement of the inert scan controls, the bundled Playfair Display serif actually rendering rather than a system-font fallback) has never been watched end-to-end in this environment. See progress/history.md for the full session log. PRIOR notes (spec-writer's pre-implementation summary, from the spec_ready gate) preserved below. Registered 2026-08-05 from four human-supplied mockups (login mobile/web, scan mobile/web). The app has no theme layer today -- every screen hardcodes its own hex values. Ships: (a) a shared design-token module (color/type/geometry/elevation, structured so a dark theme can be added later without touching call sites), (b) the shared primitives the mockups imply (BrandMark, PrimaryButton, SecondaryButton, restyled Field/FormField, OrDivider, StatusPill), (c) the login screen restyled for mobile and web, (d) the scan screen's VISUAL SHELL for mobile and web, and (e) an i18n layer carrying Spanish + English copy for these screens (the language-PICKER UI is 007). AUTHORITATIVE DESIGN SOURCE: docs/design-brief-visual-identity.md -- the mockup images are not in the repo, that file is their transcription; implement against it, and read its section 7 before assuming anything the mockups show is in scope. HARD CONSTRAINTS: no camera import/capture/recognition on /scan (004-home-scan-shell FR-005 and its source-inspecting test stay green -- appearance only); recent-scans rows are static local placeholder content, no API call; nav stays THREE destinations (the mockups' Cartera/Trades are unspecced features -- restyle nav, do not extend it); no dark mode (the mockups' Dark/Mobile/Web pills and '?' button are mockup-tool chrome, not app UI); 005-login's FR-006 (no useRouter on successful sign-in) and its forgot-password-as-local-state behavior are UNCHANGED by the restyle. Platform splits use the .web.tsx convention, not inline Platform.OS conditionals (Constitution I). SPEC/PLAN/TASKS WRITTEN 2026-08-05 by spec-writer -- zero [NEEDS CLARIFICATION] markers, so no /speckit-clarify pass was needed; status flipped to spec_ready, awaiting human approval at the gate. Three design decisions recorded as defaults (not blocking markers), each flagged for explicit human confirmation/override at the approval gate, per the 004/005 precedent: (1) FONT -- no serif is reliably bundled/consistent across iOS/Android/web (Android's generic 'serif' family's bold weight is frequently synthetic), so the plan bundles Playfair Display 700 via @expo-google-fonts/playfair-display + expo-font rather than faking a bolded sans or accepting three different system serifs; Lora is named as the most plausible override. (2) CONTRAST -- computed (not eyeballed) WCAG ratios found four of the brief's Section 2.1 values miss 4.5:1: text.secondary on bg.page (4.12:1), text.placeholder on bg.surface (2.54:1 -- and a second finding beyond the brief's own ask: the SAME token is also used for the viewfinder's dark-background hint text, and no single gray clears 4.5:1 against both a near-white and a near-black surface, so it's split into two tokens), text.link across every background it's used on (2.93-3.43:1), and accent.priceGreen on bg.surface (3.33:1). Adjusted values recorded in spec.md's Clarifications with a before/after table, and a new src/theme/contrast.test.ts regression-guards them going forward rather than leaving this as a one-time manual calculation. (3) SCAN SHELL CHROME -- the brief describes 'the existing sidebar'/'the existing bottom tab bar' around /scan, but 004-home-scan-shell deliberately built /scan OUTSIDE the three-destination shell (it has its own standalone 'Back' affordance specifically because it has no persistent nav of its own); the spec's recommended default keeps /scan a standalone route exactly as 004 built it (no sidebar/tab bar added, restyled 'Back' affordance stays the sole return path) rather than either moving the route into the shell (real expo-router-version risk on native, a bigger structural diff than 'visual shell only') or building a second, duplicate-logic look-alike nav rail -- the alternative (moving /scan into the shell) is fully documented as an override option. Also disclosed as an accepted side effect, not a hidden one: per the explicit instruction to extend src/features/identity/FormField.tsx rather than add a parallel component, restyling it in place will also visually change RegistrationForm/VerifyPhoneScreen/ProfileForm's rendered field appearance (labels/pill shape) even though this feature's scope is login+scan only -- tasks.md's Polish phase re-runs the full existing test suite specifically to confirm this has no behavioral regression. i18n mechanism is a small hand-rolled lookup (src/domain/i18n/translate.ts + per-screen es/en dictionaries + a React context/hook in src/features/i18n/), not i18next -- justified in plan.md as proportionate to today's two-screen, no-interpolation scope, with the seam (useLocale().setLocale) 007-localization builds its picker on top of. Default locale hardcoded to Spanish (spec.md FR-012), explicitly flagged as a 007-localization placeholder, not a permanent decision. specs/006-visual-identity/spec.md: 4 user stories (P1 shared token/primitive foundation, P1 i18n layer -- both Foundational, P2 login restyle, P3 scan visual shell), FR-001..FR-014, SC-001..SC-006, three Clarifications entries with full rejected-alternatives tables. plan.md: Constitution Check all PASS (V has the same kind of documented, narrow cross-cutting exception 004's src/features/navigation/ already established, for the new src/theme/ and src/features/ui/ modules), Research Decisions covering the token module's platform-split-only-for-elevation structure, the contrast regression test, font loading (reusing the existing KycGate loading-guard pattern, no new splash-screen dependency), the login screen's chrome-is-a-wrapper-not-a-screen-fork design (keeping 005's entire FR-006-guarded state machine in one file), the forgot-password sub-views inheriting the token vocabulary with no new mockup-specified layout, the FormField-stays-FormField decision and its disclosed side effect, ScanPlaceholderScreen's retirement in favor of ScanShellScreen (+ .web.tsx) with the camera-import guard test migrated intact, and the i18n mechanism choice. tasks.md: ~54 tasks across Setup (2 new expo-font/expo-linear-gradient deps + 1 pure-JS font package), a large Foundational phase (token module, six primitives, i18n layer), then User Story 2 (login) sequenced before User Story 3 (scan) per the stated higher-regression-risk-first ordering, then Polish (a full-suite regression run explicitly named to catch the FormField side effect, plus ./init.sh). HUMAN APPROVAL GATE: status is now spec_ready; per AGENTS.md, sdd-orchestrator must pause here for explicit human go-ahead -- and should specifically relay all three flagged Clarifications decisions (font choice, the four adjusted contrast values, and /scan staying outside the shell) for confirmation, not just scope."
    },
    {
      "id": "007-localization",
      "sdd": true,
      "status": "pending",
      "branch": "007-localization",
      "spec_dir": "specs/007-localization",
      "created": "2026-08-05",
      "updated": "2026-08-05",
      "blocked_reason": null,
      "notes": "Registered 2026-08-05 alongside 006-visual-identity, at the human's request ('spanish and english if is necessary add a feature to choose the language'). 006 introduces the i18n layer and ships es+en copy for login and scan ONLY. This feature completes the job: translate the remaining existing screens (register, verify-phone, profile, KYC status, tutorial, home, nav destination labels), add the language-picker UI and its persistence, and decide/implement default-locale detection. DEPENDS ON 006 -- do not start before its i18n layer exists, or the two will define competing string mechanisms."
    },
    {
      "id": "008-scan-experience",
      "sdd": true,
      "status": "done",
      "branch": "008-scan-experience",
      "spec_dir": "specs/008-scan-experience",
      "created": "2026-08-05",
      "updated": "2026-08-05",
      "blocked_reason": null,
      "notes": "DONE 2026-08-05 -- all 38 task checkboxes (T001-T037 plus T020a) [X], reviewed batch-by-batch by code-reviewer across 9 review rounds (two returned CHANGES_REQUESTED and were fixed before proceeding), ./init.sh RESULT: SUCCESS (10/10 stages, only the pre-existing expo-doctor / native-dep-alignment warnings that predate this feature), tsc clean, 476/476 tests passing across 72 suites. Ships: the five-destination shell (Inicio/Escanear/Cartera/Trades/Perfil) replacing 004's three; a persistent shell-wide ShellHeader carrying four inert icon controls (hand-drawn Mexico/USA flag badges, currency, notifications, messages); the Escanear screen in both variants -- mobile with the branded viewfinder and an enabled local scan trigger, web deliberately WITHOUT any viewfinder/scan-button/'Camara disponible' badge -- both driven by one shared found-card state module (src/domain/scanResults.ts) and one shared FoundCardPanel; the redesigned Inicio landing screen; three new placeholder destinations; and the outright retirement of Amigos/Social. TWO REVIEW ROUNDS RETURNED BLOCKING FINDINGS, both fixed and re-approved: (1) Round 2 -- the five destination labels rendered as hardcoded Spanish from NAV_DESTINATIONS in all three nav renderers, leaving navCopy's English keys dead, so a locale switch would have left tab/sidebar names Spanish forever (FR-017/SC-006); fixed by removing the `label` field from NavDestination entirely so no render-ready hardcoded string survives, plus useTranslation(navCopy) at all three sites, with locale-switch tests confirmed to fail without the fix. (2) Round 7 -- Inicio's quick-action card rendered only a bare '+' with a hardcoded English a11y label instead of the 'Escanear una carta' that the human-confirmed Recorded default 1 names, leaving home.ts's scanQuickActionLabel dead; fixed via an additive optional `label` prop on ScanEntryCard. code-reviewer caught this same 'half-updated leftover' class FOUR times across the feature (backLabel orphaned keys, navigation/README.md describing deleted screens, scanQuickActionLabel, social/README.md) -- worth knowing as a recurring pattern for future features. HUMAN DECISIONS MADE DURING THE FEATURE: both of spec.md's recorded defaults confirmed at the approval gate (Inicio content = Option A; Amigos/Social retired outright); and mid-implementation, after being shown code-reviewer's honest assessment, the human chose to replace the shipped 'MX'/'US' lime text chips with hand-drawn flag shapes in real national colors (MX green/white/red bands, US stripes + blue canton) -- still no new dependency, no image asset, and no emoji (Android OEM emoji fonts degrade flag emoji to bare letters). ORCHESTRATOR DECISION: spec.md User Story 3 AS4 ('navigate away and back resets to idle') was owned by NO task in tasks.md -- T018's traceability cites AS1-AS3 only. The implementer found @react-navigation/bottom-tabs defaults unmountOnBlur:false, so the native Escanear screen would have kept its found state across a tab switch. Closed as T020a with unmountOnBlur:true scoped to the Escanear <Tabs.Screen> only, after code-reviewer independently confirmed it fixes AS4 with zero test ripple. ACCEPTED SIDE EFFECT: this remounts the whole screen subtree on tab-blur, so scroll position and ANY future local state on that screen also reset. VERIFICATION, PRECISELY STATED -- DO NOT OVERSTATE THIS: every one of this feature's five destinations sits behind the KYC gate inside the (app) route group, and .env has EMPTY EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY, so resolveKycRoute() returns 'unauthenticated' and every authenticated route redirects to /login. NOTHING THIS FEATURE SHIPS HAS EVER BEEN SEEN RENDERING on a real browser, simulator, or device. The orchestrator attempted a temporary uncommitted gate-bypass probe to screenshot the real DOM; browser navigation and JS execution were both blocked by a permission classifier, and the probe was reverted (app/_layout.tsx confirmed at an empty diff against main). The four manual-smoke tasks (T020, T024, T026, T032) and the T033/T034 a11y+responsive passes are therefore marked [X] on their STATICALLY verifiable parts only -- source review, RNTL assertions, StyleSheet.flatten() tap-target checks, clean three-target bundle exports -- and are GENUINELY UNVERIFIED live. No screen reader was ever run. No real tap target was ever measured. No viewport was ever really resized. Android has never been available in this environment at any point. This is the same class of gap that let 006-visual-identity's /scan shell ship on component tests alone, and it is exactly how 004-home-scan-shell's two runtime-only bugs (landscape card clipping, Dynamic Island overlap) were caught by a real device and by nothing else. A CONSOLIDATED, SCREEN-BY-SCREEN HAND-OFF LIST OF WHAT A HUMAN MUST CONFIRM ON REAL HARDWARE IS IN progress/review_008-scan-experience.md SECTION 7 (final round) -- work through it once real Supabase credentials exist, before trusting this feature in production. WHAT THE THREE STANDING INVARIANTS ACTUALLY SHOW (re-verified independently by the orchestrator, not merely reported): zero live camera-module import under src/features/scanner/ (all grep hits are comments or the source-inspection test guards themselves); `git diff main` on src/domain/kyc-gate.ts, src/features/identity/useKycGate.ts and app/_layout.tsx is EMPTY (FR-014 held); and no live import of any retired Amigos/Social component survives (remaining grep hits are prose comments only). NOT COMMITTED AND NOT PUSHED -- the branch 008-scan-experience holds the work as uncommitted changes at the human's discretion; no PR was opened. PRIOR notes preserved below. HUMAN APPROVED 2026-08-05 at the spec_ready gate: \"go ahead with both defaults, start implementing\". Both of spec.md Clarifications' recorded defaults are CONFIRMED, not overridden -- (1) Inicio content = Option A (BrandMark + display title + the repurposed quick-action card routing to Escanear via NAV_DESTINATIONS, no user-specific data); (2) Amigos/Social = Option A, retired outright (routes, screens, tests, AmigosQuickAccessPill all deleted; src/features/social/ back to README-only). Implementation may proceed without revisiting either. SPEC/PLAN/TASKS WRITTEN 2026-08-05 by spec-writer -- zero blocking [NEEDS CLARIFICATION] markers, so no /speckit-clarify pass was needed; status flipped to spec_ready, awaiting human approval at the gate. Read specs/008-scan-experience/{spec.md,plan.md,tasks.md} for full detail; this is a summary only. The four decisions the human had already settled (5-destination nav replacing amigos/home/social; web Escanear has no viewfinder/button; the found-card panel is inert UI over hardcoded sample cards; the four icon controls move shell-wide) were treated as given inputs, not reopened. TWO FURTHER DECISIONS recorded with a chosen default and EXPLICITLY FLAGGED FOR HUMAN CONFIRMATION AT THE APPROVAL GATE (spec.md Clarifications): (1) Inicio's redesigned content/layout, since no mockup was supplied for it -- default proposal is BrandMark + serif title/tagline + the existing '+' ScanEntryCard repurposed as a quick-action shortcut into the new Escanear destination, reusing 004's already-tested press-to-navigate affordance rather than inventing a new one; (2) Amigos and Social are RETIRED OUTRIGHT (route files, placeholder screens, tests, and HomeScreen's AmigosQuickAccessPill all removed, not just unlinked) -- and, tracing resolveKycRoute()/useKycGate()/KYC_ROUTE_TARGETS directly (src/domain/kyc-gate.ts, src/features/identity/useKycGate.ts), confirmed the 5-destination change needs ZERO changes to any of the three: KYC_ROUTE_TARGETS' type already excludes 'main' as a key and app/_layout.tsx's KycGate never redirects on it, exactly the same reason 004-home-scan-shell's FR-009 zero-diff was possible. A THIRD, STRUCTURALLY SIGNIFICANT FINDING, not a blocking question but disclosed prominently in both spec.md and plan.md: Escanear moving into the persistent shell (settled decision 1) directly REVERSES 006-visual-identity's own Recorded default 3, which had deliberately kept /scan a standalone route outside the shell with its own manual 'Back' affordance -- that decision predates the 5-destination mockups and is superseded by them; app/scan.tsx and its Back affordance are retired, replaced by app/(app)/escanear.tsx as one of the five persistent destinations like every other. Several smaller mockup-transcription ambiguities were resolved inline as non-blocking Design notes (matching 004/006's established pattern): dropping the web 'Cámara disponible' badge alongside the viewfinder/button it describes (a direct extension of settled decision 2's own reasoning, since keeping a camera-availability badge with no camera would be misleading); the found-state trigger mechanism (button press on mobile, search-field submit or upload-dropzone tap on both platforms -- decision 3 required local interactions but never specified what triggers them); the sample-card pool (the three mockup-specified cards -- Dragón Eterno/Fénix de Tormenta/Serpiente del Vacío -- become the SINGLE shared source for both the found panel and RecentScansList, replacing 006's unrelated Charizard/Blastoise/Venusaur placeholder set that predated this mockup; two of the three cards' set labels are spec-writer-invented since only Dragón Eterno's detail panel was transcribed, flagged explicitly as low-stakes); the ambiguous 'Gradeada'-field toggle transcription; and the language control's flag icons deliberately avoiding raw flag emoji (stock Android does not reliably render regional-indicator flag sequences as flags -- a real, disclosed platform gap, not hypothetical), using a small hand-drawn two-letter badge instead, with zero new icon/asset dependency. Backend cross-check performed directly against the sibling Draw-a-card repo's feature_list.json (2026-08-05): no portfolio/trading/profile-view bounded-context feature exists there yet, so Cartera/Trades/Perfil introduce no field/entity shape to keep in sync with anything today -- a future feature that gives them real content owns that cross-check when it happens; the backend's 008-catalog-search (done) is a plausible future real data source for Escanear's search field, deliberately NOT called by this feature per settled decision 3. specs/008-scan-experience/spec.md: 6 user stories (US1 shell+shared header P1/Foundational, US2 found-state domain logic P1/Foundational, US3 mobile Escanear P1, US4 web Escanear P1, US5 Inicio redesign P2, US6 Cartera/Trades/Perfil placeholders + Amigos/Social retirement P3), FR-001..FR-018, SC-001..SC-007. plan.md: Constitution Check all PASS (reuses 004's src/features/navigation/ and 006's src/features/scanner/ Constitution V exceptions, adds no new one; Cartera/Trades/Perfil land in the already-scaffolded src/features/portfolio//trading/ and src/features/identity/), Research Decisions covering the Escanear-into-the-shell reversal, the found-card state machine's src/domain/scanResults.ts + a thin useScanSimulation() RN wrapper (mirroring 006's translate()/LocaleContext split), the shared sample-card pool, the ShellHeader single-shared-component approach (three consumption points: native Tabs header, WebSidebarNav, WebBottomBarNav -- zero per-screen duplication), the icon-only controls/flag-badge design, and the placeholder-screen reuse pattern (now localized, unlike 004's originals). tasks.md: 37 tasks (T001-T037) across a Foundational phase (domain/i18n + shell chrome + found-state logic, T001-T014), four P1 user-story phases (T015-T026), a P3 phase (T027-T032), and Polish (T033-T037, ending in a full regression suite run + ./init.sh). HUMAN APPROVAL GATE: status is now spec_ready; per AGENTS.md, sdd-orchestrator must pause here for explicit human go-ahead -- and should specifically relay the two flagged Clarifications decisions (Inicio's proposed content; Amigos/Social's outright retirement) plus the disclosed reversal of 006's 'Escanear stays outside the shell' decision, not just scope confirmation. Also worth relaying: this feature's branch has not yet been cut -- confirm 006-visual-identity's two commits (16d8620, 44c6cc4) are on `main` before cutting it (they were not yet merged as of this feature's registration; verify at approval time, not assumed still true). PRIOR (kickoff) notes preserved below. Registered 2026-08-05 at the human's request: \"lets create a new feature, the scaner view need to be like the images uploaded, keep in mind that the web version it won't have the scaner option, also add the icons for localization (spanish/english with mexican/USA flags), currency(usd,mxn) notifications and messages, only the icons the implementation needs to be on other feature and redesing the pre-scanner view (feature 004-home-scan-shell) this will be the first view when a user logs (after tutorial) put it with the same design as the other views\".\n\nDEPENDS ON 006-visual-identity (theme tokens in src/theme, i18n layer in src/domain/i18n + src/features/i18n, and the existing scan shell in src/features/scanner). At registration time 006 was `done` but its two commits (16d8620, 44c6cc4) were NOT yet merged into main -- this feature's branch must be cut only after that merge lands, or the design tokens it depends on will not exist on the branch base.\n\nFOUR DECISIONS ALREADY ANSWERED BY THE HUMAN (2026-08-05, at the orchestrator's asking -- treat these as settled, do NOT re-open them as [NEEDS CLARIFICATION]):\n1. NAVIGATION: REPLACE the shell's current three destinations with the mockup's FIVE -- Inicio, Escanear, Cartera, Trades, Perfil. src/domain/navigation.ts's NAV_DESTINATIONS (currently amigos/home/social) is rewritten. The existing Amigos and Social screens are retired or folded elsewhere; Cartera, Trades and Perfil become new EMPTY PLACEHOLDER destinations (they have no content yet and none is in scope here).\n2. WEB HAS NO CAMERA SCANNER: despite the web mockup drawing a viewfinder, web must NOT render the viewfinder or the \"Escanear carta\" button. On web the Escanear destination keeps ONLY the search field + image-upload affordance (plus the results/detail panel). The Escanear destination itself REMAINS in the web sidebar. This is a deliberate deviation from the supplied web mockup -- record it as such in spec.md so it is not later mistaken for an implementation bug. Use the .web.tsx convention (Constitution IV), not inline Platform.OS branches -- ScanShellScreen.web.tsx already exists as the split point.\n3. SCOPE OF THE RESULT PANEL: INERT UI WITH MOCK DATA. Build the full found-card layout and its LOCAL interactions (condition chip selection, quantity +/- stepper, graded toggle) driven by HARDCODED sample cards. NO camera module, NO recognition, NO backend calls anywhere in this feature -- same discipline 004 and 006 held. The \"found\" state is reachable only as a local UI state, not from a real scan.\n4. THE FOUR ICONS GO TOP-RIGHT ON EVERY SCREEN: a persistent header row inside the app shell (visible on Inicio, Escanear and the rest), not just on the home screen as today. Icons only -- language (Mexican/USA flags for es/en), currency (USD/MXN), notifications, messages. They stay INERT: no language switching (that is 007-localization's job), no currency conversion, no notification feed, no messaging. This REPLACES src/features/navigation/TopRightControls.tsx's current text-label stubs (ENG/ESP, USD/MXN, Notifications, Messages) and moves them out of HomeScreen into the shell.\n\nALSO IN SCOPE: redesign the pre-scanner / landing view that 004-home-scan-shell shipped (src/features/navigation/HomeScreen.tsx -- currently the top-left Amigos pill + top-right text controls + a centre \"+\" card). It becomes the Inicio destination and the FIRST screen a user lands on after login/tutorial, restyled in the same visual language as the mockups. NO mockup was supplied for Inicio itself -- the spec should propose its content (a reasonable default: the same 006 visual language, page title, and whatever affordances survive from 004's \"+\" card) and flag that proposal explicitly for the human at the approval gate rather than inventing scope silently. Confirm the post-login/post-tutorial landing route against 001-registration-kyc's resolveKycRoute()/useKycGate()/KYC_ROUTE_TARGETS before changing any routing (004 FR-009 kept that gate at zero diff -- do the same unless the 5-destination change genuinely forces a change, and say so if it does).\n\nTHE SUPPLIED MOCKUPS (4 images, transcribed here because the images themselves are not in the repo -- the human has them; ask before guessing beyond this):\n- IMAGE 1, MOBILE \"Escanear\", idle: display-serif title \"Escanear\"; a near-black rounded viewfinder with a faint grid, four lime corner brackets, a camera glyph + \"Apunta la cámara a la carta\", and a small circular dark gear button inset at its top-right; a white rounded search field, placeholder \"Buscar carta por nombre o código...\" with a magnifier at the right; a dashed-border rounded upload dropzone \"Subir imagen de carta\"; a full-width lime primary button \"Escanear carta\" in bold serif; page background light grey, surfaces white. Bottom tab bar with five items and icons: Inicio (home), Escanear (scan-frame, ACTIVE = lime), Cartera (briefcase), Trades (up/down arrows), Perfil (person).\n- IMAGE 2, WEB \"Escanear\", idle, three regions: LEFT SIDEBAR -- a lime rounded logo tile \"D\" + serif wordmark \"Draw a Card\" + subtitle \"v2.0 · PLD/AML\"; icon+label nav rows Inicio / Escanear (ACTIVE = lime-tinted pill with lime text) / Mi Cartera / Trades / Mi Perfil; a bottom user block with a \"JD\" avatar circle, \"Juan Doe\", \"@juandoe · Free\". CENTRE COLUMN -- page title \"Escanear carta\" with a small lime-tinted pill badge \"Cámara disponible\", then viewfinder, search field, upload dropzone, lime \"Escanear carta\" button. (Per decision 2 the viewfinder, the badge and the button do NOT ship on web.) RIGHT COLUMN -- an empty-state dashed rounded panel with a card glyph, \"Escanea una carta para ver sus detalles aquí\" / \"Los resultados aparecerán automáticamente\"; below it an uppercase letterspaced section label \"ESCANEOS\" and a list of past-scan rows, each a rounded gradient thumbnail + bold serif name + muted \"PSA 10 · GEN-001\" line + a right-aligned green price. Sample rows: \"Dragón Eterno / PSA 10 · GEN-001 / $45,000\", \"Fénix de Tormenta / BGS 9.5 · ARC-047 / $12,500\", \"Serpiente del Vacío / PSA 9 · GEN-022 / $8,900\". A floating circular \"?\" help button sits bottom-right.\n- IMAGE 3, WEB, found state: the viewfinder shows a glowing horizontal lime scan line, a check glyph and \"¡Carta encontrada!\"; the right column's empty state is REPLACED by the card-detail panel -- thumbnail (purple gradient + dragon glyph), \"Dragón Eterno\" bold serif, muted \"Genesis · GEN-001\", a row of two pills (solid lime \"PSA 10\", green-tinted \"$45,000\"), and two text links \"Eliminar\" (red) · \"Cambiar\" (blue); a divider; two side-by-side fields \"Gradeada\" and \"PSA 10\" the latter carrying an OFF toggle switch; a \"Condición actual\" label above a chip row Near Mint (SELECTED, lime) / Excellent / Very Good / Good / Fair; a \"Cantidad\" row with a − 1 + stepper; a \"Precio de mercado\" row with a green \"$45,000\"; and a full-width lime \"Aceptar\" button. In this state the centre column's \"Escanear carta\" button is gone while the search field and upload dropzone remain.\n- IMAGE 4, MOBILE, found state: same found viewfinder; below it the search field and upload dropzone, then the SAME card-detail panel stacked inline in the single column (not a side panel), with the condition chips wrapping onto a second row (Near Mint / Excellent / Very Good / Good, then Fair). Bottom tab bar as in image 1.\nNOTE: the \"Dark / Mobile / Web\" pill toggles at the top-right of images 2-4, and the floating \"?\" button, are the human's MOCKUP TOOL CHROME -- they are NOT app UI and must not be built. All mockup copy is Spanish; en strings are still required via 006's i18n layer (007-localization owns the picker, not this feature).\n\nEXISTING CODE THIS TOUCHES (verified on disk 2026-08-05, not assumed): src/domain/navigation.ts (NAV_DESTINATIONS, SCAN_ROUTE, BREAKPOINT_PX, resolveWebNavLayout); app/(app)/_layout.tsx (native <Tabs>) and app/(app)/_layout.web.tsx; src/features/navigation/{HomeScreen,TopRightControls,AmigosQuickAccessPill,WebSidebarNav,WebBottomBarNav}.tsx; src/features/scanner/{ScanShellScreen,ScanShellScreen.web,Viewfinder,ScanSearchField,UploadDropzone,ScanEntryCard,RecentScansList,EmptyResultsPanel}.tsx; app/scan.tsx and app/(app)/{index,amigos,social}.tsx; src/domain/i18n/copy/scan.ts; src/theme/*. RecentScansList and EmptyResultsPanel already exist from 006 -- check what they already do before rebuilding them."
    }
  ]
}

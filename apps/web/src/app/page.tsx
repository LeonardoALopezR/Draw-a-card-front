export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-2xl font-semibold">Draw-a-card</h1>
      <p className="text-gray-500">
        Scaffold ready. Implement screens per spec — see <code>specs/</code> at the project
        root, starting with registration/KYC to match the backend&apos;s
        <code> 001-user-registration-kyc</code> spec.
      </p>
    </main>
  );
}

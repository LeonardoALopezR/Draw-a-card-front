# Catalog feature module

Mirrors the backend's `catalog` module. Components, hooks, and API calls for this domain
live here. See `specs/` at the project root for the spec that defines what belongs here
before adding new screens/components to this folder.

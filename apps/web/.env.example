# Backend API (from the Draw-a-card backend repo, running locally via docker compose)
NEXT_PUBLIC_API_URL="http://localhost:3000"

# Auth provider (must match the backend's configured provider)
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

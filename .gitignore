node_modules/
dist/
.expo/
web-build/
.env
.env.local
*.log
.DS_Store

# specs/015-ci-test-timeout: jest's babel transform cache (jest.config.js's cacheDirectory).
# Local build artifact, never committed; CI persists its own copy via actions/cache instead.
.jest-cache/

# Agent folders may contain credentials/session artifacts — never commit
.claude/settings.local.json

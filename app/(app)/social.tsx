// T014 (specs/004-home-scan-shell): the Social destination's route file — renders
// SocialPlaceholderScreen (src/features/social/SocialPlaceholderScreen.tsx, T006) only, no
// business logic here (Constitution IV).
import { SocialPlaceholderScreen } from "@/features/social/SocialPlaceholderScreen";

export default function SocialRouteScreen() {
  return <SocialPlaceholderScreen />;
}

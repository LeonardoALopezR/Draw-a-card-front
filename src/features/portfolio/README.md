# Portfolio feature module

Mirrors the backend's `portfolio` module. Screens/components for this domain live here,
built on top of `src/domain` (portable logic) and `src/lib` (Expo-specific wiring). See
`specs/` at the project root for the spec defining what belongs here.

## Current contents

`CarteraPlaceholderScreen.tsx` (specs/008-scan-experience T027) — this module's first real
file, a contentless placeholder reserving the Cartera shell destination (`app/(app)/cartera.tsx`)
for a future portfolio feature. No portfolio/inventory data or logic exists here yet.
